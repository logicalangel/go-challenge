version: "3"
services:
  uss:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_PATH: ./cmd/uss
    restart: always
    depends_on:
      - "es"
  es:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_PATH: ./cmd/es
    restart: always
